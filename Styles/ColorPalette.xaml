<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ==========================================
         НОВАЯ ЦВЕТОВАЯ ПАЛИТРА CMYK
         Основные цвета: #00c0a5, #005c4d, #c0bfbf, #363636
    =========================================== -->

    <!-- Базовые цвета (Solid Color Brushes) -->
    <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#00c0a5"/>
    <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#005c4d"/>
    <SolidColorBrush x:Key="PrimaryLightBrush" Color="#c0bfbf"/>
    <SolidColorBrush x:Key="PrimaryDarkGrayBrush" Color="#363636"/>

    <!-- Дополнительные оттенки -->
    <SolidColorBrush x:Key="PrimaryAccentLightBrush" Color="#33d4be"/>
    <SolidColorBrush x:Key="PrimaryAccentDarkBrush" Color="#008a75"/>
    <SolidColorBrush x:Key="PrimaryDarkLightBrush" Color="#1a7a68"/>
    <SolidColorBrush x:Key="PrimaryLightDarkBrush" Color="#a6a6a6"/>

    <!-- Текст -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="#c0bfbf"/>
    <SolidColorBrush x:Key="TextDarkBrush" Color="#363636"/>

    <!-- Фоновые цвета -->
    <SolidColorBrush x:Key="BackgroundMainBrush" Color="#1a1a1a"/>
    <SolidColorBrush x:Key="BackgroundSurfaceBrush" Color="#2d2d2d"/>
    <SolidColorBrush x:Key="BackgroundCardBrush" Color="#363636"/>
    <SolidColorBrush x:Key="BackgroundInputBrush" Color="#1a1a1a"/>

    <!-- Статус цвета -->
    <SolidColorBrush x:Key="SuccessBrush" Color="#00c0a5"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="#e74c3c"/>
    <SolidColorBrush x:Key="WarningBrush" Color="#f39c12"/>
    <SolidColorBrush x:Key="OnlineBrush" Color="#00c0a5"/>
    <SolidColorBrush x:Key="OfflineBrush" Color="#c0bfbf"/>

    <!-- ==========================================
         ГРАДИЕНТЫ (LinearGradientBrush)
    ========================================== -->

    <!-- Основной градиент: #00c0a5 → #005c4d -->
    <LinearGradientBrush x:Key="PrimaryGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#00c0a5" Offset="0"/>
        <GradientStop Color="#005c4d" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для панелей: #c0bfbf → #363636 -->
    <LinearGradientBrush x:Key="SurfaceGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#c0bfbf" Offset="0"/>
        <GradientStop Color="#363636" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для кнопок (акцент) -->
    <LinearGradientBrush x:Key="ButtonAccentGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#00c0a5" Offset="0"/>
        <GradientStop Color="#33d4be" Offset="0.5"/>
        <GradientStop Color="#00c0a5" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для кнопок (темный) -->
    <LinearGradientBrush x:Key="ButtonDarkGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#363636" Offset="0"/>
        <GradientStop Color="#1a1a1a" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для заголовков -->
    <LinearGradientBrush x:Key="HeaderGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#00c0a5" Offset="0"/>
        <GradientStop Color="#005c4d" Offset="0.5"/>
        <GradientStop Color="#00c0a5" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для карточек датчиков -->
    <LinearGradientBrush x:Key="SensorCardGradientBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#2d2d2d" Offset="0"/>
        <GradientStop Color="#363636" Offset="1"/>
    </LinearGradientBrush>

    <!-- Градиент для предупреждений -->
    <LinearGradientBrush x:Key="WarningGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="#f39c12" Offset="0"/>
        <GradientStop Color="#e67e22" Offset="1"/>
    </LinearGradientBrush>

    <!-- ==========================================
         ГЛОБАЛЬНЫЕ СТИЛИ ДЛЯ ВСЕГО ПРИЛОЖЕНИЯ
    ========================================== -->

    <!-- Стиль для окна -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{StaticResource BackgroundMainBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- Стиль для кнопок -->
    <Style TargetType="Button" x:Key="ModernButtonStyle">
        <Setter Property="Height" Value="40"/>
        <Setter Property="Padding" Value="15,0"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="6"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Акцентная кнопка (градиентная) -->
    <Style TargetType="Button" x:Key="AccentButtonStyle" BasedOn="{StaticResource ModernButtonStyle}">
        <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}"/>
        <Setter Property="Height" Value="45"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Bold"/>
    </Style>

    <!-- Темная кнопка -->
    <Style TargetType="Button" x:Key="DarkButtonStyle" BasedOn="{StaticResource ModernButtonStyle}">
        <Setter Property="Background" Value="{StaticResource ButtonDarkGradientBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Стиль для TextBox -->
    <Style TargetType="TextBox" x:Key="ModernTextBoxStyle">
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush" Value="{StaticResource PrimaryAccentBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryAccentBrush}"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для ComboBox -->
    <Style TargetType="ComboBox" x:Key="ModernComboBoxStyle">
        <Setter Property="Height" Value="40"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      BorderBrush="{TemplateBinding BorderBrush}"
                                      BorderThickness="{TemplateBinding BorderThickness}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      Focusable="False"
                                      ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="border"
                                            Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <Grid>
                                            <ContentPresenter HorizontalAlignment="Stretch"
                                                              VerticalAlignment="Center"
                                                              Margin="12,0,30,0"/>
                                            <Path x:Name="Arrow"
                                                  Fill="{StaticResource TextSecondaryBrush}"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Center"
                                                  Margin="0,0,12,0"
                                                  Data="M 0 0 L 4 4 L 8 0 Z"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="Arrow" Property="Data" Value="M 0 4 L 4 0 L 8 4 Z"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <Popup x:Name="Popup"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               Placement="Bottom"
                               AllowsTransparency="True"
                               Focusable="False">
                            <Border Background="{StaticResource BackgroundCardBrush}"
                                    BorderBrush="{StaticResource PrimaryLightBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Margin="0,2,0,0">
                                <ScrollViewer>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для DataGrid -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource BackgroundMainBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="{StaticResource BackgroundCardBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource BackgroundSurfaceBrush}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource PrimaryGradientBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Height" Value="35"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
    </Style>

    <!-- Стиль для ScrollViewer -->
    <Style TargetType="ScrollViewer">
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
    </Style>

    <Style TargetType="ScrollBar">
        <Setter Property="Background" Value="{StaticResource BackgroundSurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Стиль для ListBox -->
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryLightBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="ListBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryAccentBrush}"/>
                <Setter Property="Foreground" Value="White"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Стиль для TabControl -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="Transparent"/>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="Background" Value="{StaticResource BackgroundSurfaceBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="Padding" Value="20,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0,0,0,2"
                            BorderBrush="Transparent"
                            CornerRadius="4,4,0,0">
                        <ContentPresenter x:Name="ContentSite"
                                          ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryAccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource PrimaryAccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource PrimaryAccentLightBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Стиль для ProgressBar -->
    <Style TargetType="ProgressBar">
        <Setter Property="Background" Value="{StaticResource BackgroundInputBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryGradientBrush}"/>
        <Setter Property="Height" Value="6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border Background="{TemplateBinding Background}"
                            CornerRadius="3">
                        <Border x:Name="PART_Track">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="3"/>
                        </Border>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    </ResourceDictionary>